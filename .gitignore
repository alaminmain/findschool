# === OS / editor ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# === Claude / local tooling ===
.claude/settings.local.json

# === Secrets ===
.env
.env.*
!.env.example
mobile/play-service-account.json
*.pem
*.keystore
*.jks

# === Node / Expo ===
mobile/node_modules/
mobile/.expo/
mobile/.expo-shared/
mobile/dist/
mobile/web-build/
mobile/*.log
mobile/android/
mobile/ios/

# === .NET ===
admin/**/bin/
admin/**/obj/
admin/**/*.user
admin/**/*.suo
*.db-shm
*.db-wal

# === Python ===
scraper/__pycache__/
scraper/.venv/
scraper/venv/
scraper/*.pyc

# === Large data artifacts (generated, not source) ===
# The shipped SQLite DB and raw scrape output are too large for git.
# Keep generation reproducible via scripts; share binaries via Releases / LFS.
scraper/schools_raw.csv
admin/**/find-school.db
mobile/assets/db/find-school.db

# === Rasterized brand outputs (regenerate from SVG sources) ===
mobile/assets/icon.png
mobile/assets/adaptive-icon.png
mobile/assets/splash.png
mobile/assets/favicon.png
mobile/store/play-icon.png
mobile/store/feature-graphic.png
mobile/store/screenshots/

# === EAS / build outputs ===
mobile/*.apk
mobile/*.aab
mobile/*.ipa

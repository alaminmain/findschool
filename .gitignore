# === OS / editor ===
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# === Claude / local tooling ===
.claude/

# === Secrets ===
.env
.env.*
!.env.example
mobile/play-service-account.json
*.pem
*.keystore
*.jks

# === Node / Expo ===
mobile/node_modules/
mobile/.expo/
mobile/.expo-shared/
mobile/dist/
mobile/web-build/
mobile/*.log
mobile/android/
mobile/ios/

# === .NET ===
admin/**/bin/
admin/**/obj/
admin/**/*.user
admin/**/*.suo
admin/**/geocode-cache.json

# === Python ===
scraper/__pycache__/
scraper/.venv/
scraper/venv/
scraper/*.pyc
scraper/*.png

# === Build outputs ===
# admin/FindSchool.Admin/find-school.db is regenerated by every dotnet run;
# the canonical copy lives at mobile/assets/db/find-school.db.
admin/FindSchool.Admin/find-school.db
admin/FindSchool.Admin/geocode-cache.json
fr_cht.pdf

# === Untracked data sources ===
# Drop-in CSVs / PDFs the user provides for reference but that we don't
# (yet) ingest. Keep on disk for future merge work; don't commit.
cleaned_school_data.csv
secondary-school-list.pdf

# === Large data artifacts ===
# Raw scrape CSV stays out of git (regeneratable, large, noisy diffs).
# find-school.db IS tracked so the mobile app can build straight from clone.
# If it crosses ~50 MB, switch to Git LFS or a GitHub Release asset.
scraper/schools_raw.csv
*.db-shm
*.db-wal
*.db-journal

# === Brand outputs ===
# Rasterized PNGs are tracked so the repo builds without re-running the
# rasterizer. Regenerate with `python scraper/rasterize_brand.py`.
mobile/store/screenshots/

# === EAS / build outputs ===
mobile/*.apk
mobile/*.aab
mobile/*.ipa
